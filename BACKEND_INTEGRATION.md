# Backend Integration Complete! 🎉

Frontend успешно подключен к backend API.

## ✅ Что сделано:

1. **Настроен .env.local**
   ```
   NEXT_PUBLIC_USE_MOCK=false
   NEXT_PUBLIC_API_URL=http://localhost:8000
   ```

2. **Активированы все API вызовы**
   - ✅ `lib/api/links.ts` - все CRUD операции
   - ✅ `lib/api/stats.ts` - статистика и dashboard

3. **Готовые эндпоинты**
   - `GET /api/links/` - список с фильтрацией
   - `GET /api/links/:id/` - одна ссылка
   - `POST /api/links/` - создание
   - `PATCH /api/links/:id/` - обновление
   - `DELETE /api/links/:id/` - удаление
   - `POST /api/links/bulk-update-status/` - bulk статус
   - `POST /api/links/bulk-assign-manager/` - bulk менеджер
   - `GET /api/stats/dashboard/` - общая статистика
   - `GET /api/stats/platform/:platform/` - статистика по платформе

## 🚀 Как запустить:

### 1. Запусти Backend
```bash
cd phil-backend
docker-compose up -d
```

Проверь что backend работает:
```bash
curl http://localhost:8000/api/links/
```

### 2. Перезапусти Frontend
```bash
cd phil-frontend

# Останови текущий сервер (Ctrl+C если запущен)
npm run dev
```

Открой http://localhost:3000

## 🧪 Что тестировать:

### Dashboard (главная страница)
- ✅ Статистика загружается с backend
- ✅ График активности показывает реальные данные
- ✅ Карточки платформ работают
- ✅ Клик по платформе переходит на страницу

### Platform Pages
- ✅ Таблица загружает ссылки с backend
- ✅ Фильтры работают (status, priority, manager, search)
- ✅ Создание новой ссылки через "Add Link"
- ✅ Редактирование через "..." → Edit
- ✅ Удаление через "..." → Delete
- ✅ Bulk actions (выбрать несколько, изменить статус/менеджера)

### Mobile
- ✅ Карточки вместо таблиц
- ✅ Фильтры в drawer
- ✅ Все операции работают

## 🔧 Troubleshooting

### Проблема: "Failed to fetch"
**Решение**: Проверь что backend запущен:
```bash
curl http://localhost:8000/api/links/
```

### Проблема: CORS ошибка
**Решение**: Backend должен разрешать `http://localhost:3000` в CORS настройках

### Проблема: 404 на эндпоинтах
**Решение**: Убедись что используешь правильные URL с `/api/` префиксом

### Проблема: Неправильный формат данных
**Решение**: 
- ID должны быть строками (UUID)
- Даты в ISO 8601 формате
- Проверь FRONTEND_INTEGRATION.md в backend репозитории

## 📊 Формат данных

Backend должен возвращать данные в таком формате:

**NegativeLink:**
```json
{
  "id": "uuid-string",
  "url": "https://...",
  "platform": "facebook",
  "type": "post",
  "status": "active",
  "detected_at": "2026-02-17T10:30:00Z",
  "removed_at": null,
  "priority": "high",
  "manager": "John Doe",
  "notes": "..."
}
```

**Dashboard Stats:**
```json
{
  "total": 100,
  "active": 25,
  "removed": 50,
  "in_work": 15,
  "pending": 10,
  "new_last_7_days": 12,
  "removed_last_7_days": 8,
  "platforms": [...],
  "activity_chart": [...]
}
```

## 🔄 Вернуться к mock данным

Если нужно временно вернуться к mock:

```bash
# В .env.local измени:
NEXT_PUBLIC_USE_MOCK=true

# Перезапусти frontend
npm run dev
```

## 📚 Документация

- **Backend Integration Guide**: https://github.com/intersson-sir/phil-backend/blob/main/FRONTEND_INTEGRATION.md
- **Frontend README**: README.md в этом репозитории
- **API Types**: `types/index.ts` и `types/api.ts`

## ✨ Готово!

Frontend полностью интегрирован с backend. Все функции работают с реальными данными!

Если возникнут вопросы - проверь FRONTEND_INTEGRATION.md в backend репозитории.
